// 기존 array를 변경
/**
 * push()
 * 값을 마지막에 추가하고,
 * return 값은 추가한 이후에 array 길이
 */

/**
 * pop()
 * 마지막 값을 빼내고,
 * return 값은 빼낸 값
 */
/**
 * shift()
 * 첫 번째 값을 빼내고,
 * return 값은 빼낸 값
 */
/**
 * unshift()
 * 값을 처음에 추가하고,
 * return 값은 추가한 이후에 array 길이
 */
/**
 * splice()
 * 분리
 */

// 새로운 array 생성
/**
 * concat()
 * push와 비슷
 */
/**
 * slice()
 */

// 자주 쓰이는 함수
/**
 * spread
 * ...
 * immutable 만들 때 많이 쓰임
 * 깊은 복사할 때
 */
/**
 * join()
 */
/**
 * map((x)=>{})
 */
/**
 * filter((x)=>{})
 */
/**
 * find((x)=>{})
 */
/**
 * reduce((p,n)=>파라미터 이용해서 콜백함수,초기값)
 */
